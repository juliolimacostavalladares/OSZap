<svg width="512" height="512" viewBox="0 0 512 512" fill="none" xmlns="http://www.w3.org/2000/svg">
  {/* Fundo do Favicon */}
  <rect width="512" height="512" fill="#20232B"/> {/* Cor de fundo escura */}

  {/* Grupo para o logo: centralizado e escalado para 70% */}
  {/* Vamos usar transform para escalar e centralizar o texto */}
  {/* A largura do texto "OSZap!" com font-size 65 é aproximadamente 360px */}
  {/* 70% de 512px é 358.4px. Vamos escalar para que a largura total seja ~358px. */}
  {/* A escala será 358.4 / 400 (largura original aproximada do texto no svg acima) = ~0.896 */}
  {/* Para centralizar um item de ~358.4px de largura em 512px: (512 - 358.4) / 2 = 76.8px */}
  {/* Para centralizar um item de ~65px de altura (font-size) em 512px: (512 - 65) / 2 = 223.5px */}
  
  <g transform="translate(76.8, 223.5) scale(0.896)"> 
    {/* Texto "OSZap" */}
    <text x="50" y="95" font-family="'Montserrat', sans-serif" font-size="65" font-weight="bold" fill="white">
      OSZap
    </text>

    {/* Ponto de exclamação laranja */}
    <text x="400" y="95" font-family="'Montserrat', sans-serif" font-size="65" font-weight="bold" fill="#F97316">
      !
    </text>
  </g>
</svg>